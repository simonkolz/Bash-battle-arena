#!/bin/bash

# Creates a new directory called Arena. (If it doesnt already exists,)
mkdir -p Arena

# Moves into the Arena directory.
cd Arena

# Creates three empty files: warrior.txt, mage.txt, and archer.txt
touch warrior.txt mage.txt archer.txt

# Lists the files in the current directory (which is now Arena).
ls
